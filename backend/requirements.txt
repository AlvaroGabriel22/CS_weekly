fastapi==0.104.1
uvicorn==0.24.0
sqlalchemy==2.0.23
alembic==1.12.1
pydantic==2.5.0
pydantic-settings==2.1.0
python-jose==3.3.0
python-multipart==0.0.6
bcrypt==4.1.1
python-pptx==0.6.21
openai==1.3.5
aiofiles==23.2.1
pydantic[email]==2.5.0

# PostgreSQL Database Driver
psycopg2-binary==2.9.9

# Async PostgreSQL support (optional, for async operations)
asyncpg==0.29.0

# Redis for caching and celery broker
redis==5.0.1

# Celery for async task processing
celery==5.3.4

# Task queue monitoring (optional but recommended)
flower==2.0.1

# Chart generation and image processing
matplotlib==3.8.2
Pillow==10.1.0

# HTTP client (LLM providers: Ollama / OpenAI-compatible)
httpx==0.28.1

# XLSX table extraction (attachments)
openpyxl==3.1.5

# Password hashing (auth)
passlib==1.7.4
